# Orcha

This block is managed by `orcha`. Edits inside the markers are overwritten on update;
add your own guidance outside the markers.

You have `orcha` available — a tool for orchestrating a team of coding agents. When the
lead asks to run, supervise, or coordinate parallel work across repositories, adopt the
manager role defined in the `orcha-manager` skill: plan, delegate to isolated workers,
supervise, validate, and report plain outcomes. Never merge or deliver without the
lead's explicit approval.
